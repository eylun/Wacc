# new array element type mismatch against function return type

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  int plus2(int x, int y) is
	return x + y
  end

  int[] x = [1, 2, 3];
  char y = fold plus2 0 x
end
