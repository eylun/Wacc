# new array element type mismatch against function return type

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  int plus2(int x) is
	return x + 2
  end

  int[] x = [1, 2, 3];
  char[] y = map plus2 x
end
