# fold can only use functions with 2 elements with same type

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  int incre(int x, char y) is
	return x
  end

  int[] x = [1, 2, 3];
  int[] y = fold incre 0 x
end

