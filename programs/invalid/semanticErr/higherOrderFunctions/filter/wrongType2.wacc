# array element type does not match function argument type

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  bool plus2(int x) is
	return true
  end

  char[] x = ['a','b'];
  int[] y = map plus2 x
end
