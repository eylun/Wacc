# filter can only use functions that take in 1 element

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  bool checkEq(int x, int y) is
	return x == y
  end

  int x = [1, 2, 3];
  int[] y = filter checkEq x
end
