# filter can only use functions return bool

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  char checkEq(int x, int y) is
	return x == y
  end

  int x = [1, 2, 3];
  int[] y = filter checkEq x
end
