# type of new array not equal to type of previous array

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  bool plus2(int x) is
	return true
  end

  int[] x = [1, 2, 3];
  char[] y = map plus2 x
end
