# define and call a function with a simple loop

# Input:

# Output:
# 10

# Exit: 0

# Program:

begin
  int f(int n) is
    int i = 0;
    while i < n do
      i = i + 1
    done;
    return i 
  end
  int x = call f(10) ;
  println x 
end
