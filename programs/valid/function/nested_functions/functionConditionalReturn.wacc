# program has function which only contains an if statement and a return in each branch

# Input:

# Output:
# true

# Exit: 0

# Program:

begin
  bool f() is 
    if true then
      return true
    else
      return false
    fi
  end
  bool x = call f();
  println x
end
