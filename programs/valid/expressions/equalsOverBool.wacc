# evaluating equality and boolean operator precedence

# Input:

# Output:
# true
# false

# Exit: 0

# Program:

begin
  bool p = true ;
  bool q = true ;
  bool r = false ;
  bool s = true ;
  println p && q != r || s ;
  println (p && q) != (r || s)
end
