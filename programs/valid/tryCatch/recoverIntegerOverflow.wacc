# Recover from integer overflow
# Input:

# Output:
# #runtime_error#

# Exit: 0

# Program:

begin
  int x = 2147483 ;
  try
    x = x * 1000 ;
    x = x * 1000 ;
    x = x * 1000 ;
	println x
  catch(string e)
    x = x * 1000 ;
    x = x * 1000 ;
	println x
  end
end